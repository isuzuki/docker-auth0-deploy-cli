# docker-auth0-deploy-cli

## Usage
```
docker run --rm --interactive --tty \
  --volume $PWD:/app \
  isuzuki/auth0-deploy-cli
```

## References
|Software|document|
| --- | --- |
|Auth0 Deploy CLI|https://github.com/auth0/auth0-deploy-cli|
